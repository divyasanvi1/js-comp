//switch
const month=3;
switch (month) {
    case 1: // case "jan"
        console.log("jan");
        break;
    case 3:
        console.log("mar");
        break;
    default:
        break;
}
// if there will no break then all cases below it will get printed except the default case
